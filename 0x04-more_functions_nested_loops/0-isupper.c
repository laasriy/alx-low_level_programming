#include "main.h"

/**
 * _isupper - checks for uppercase character
 * c tha variable we want to check
 * Return: 1 if c is uppercase, 0 if not
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
